<div class="flex-1">

    <h3>{{ 'admin.zoneStorage.changeLabel.titlePage'|trans({ '%zoneLabel%': zoneStorage.label }) }}</h3>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.label) }}

    <div class="grid">
        <twig:Form:Button:Cancel href="{{ path('admin_zone_storages_index') }}" label="Annuler" />
        <twig:Form:Button:Submit label="{{ 'admin.zoneStorage.changeLabel.button'|trans }}" icon="fa-pen-to-square" />
    </div>

    {{ form_end(form) }}

</div>
