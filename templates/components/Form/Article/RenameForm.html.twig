<div class="flex-1">

    <h3>Rename "{{ article.name }}"</h3>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}

    <div class="grid">
        <twig:Form:Button:Cancel href="{{ path('admin_articles_index') }}" label="Annuler" />
        <twig:Form:Button:Submit label="Rename" />
    </div>

    {{ form_end(form) }}

</div>
