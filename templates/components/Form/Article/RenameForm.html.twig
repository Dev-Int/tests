<div class="flex-1">

    <h3>{{ 'admin.article.rename.titlePage'|trans({ '%articleName%': article.name }) }}</h3>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}

    <div class="grid">
        <twig:Form:Button:Cancel href="{{ path('admin_articles_index') }}" label="Annuler" />
        <twig:Form:Button:Submit label="{{ 'admin.article.rename.button'|trans }}" icon="fa-pen-to-square"/>
    </div>

    {{ form_end(form) }}

</div>
