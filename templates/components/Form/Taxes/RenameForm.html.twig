<div class="flex-1">

    <h3>{{ 'admin.tax.rename.titlePage'|trans({ '%taxName%':tax.name }) }}</h3>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.uuid) }}

    <div class="grid">
        <twig:Form:Button:Cancel href="{{ path('admin_taxes_index') }}" label="{{ 'cancel'|trans }}" turboFrame="_top" />
        <twig:Form:Button:Submit label="{{ 'admin.tax.rename.button'|trans }}" icon="fa-pen-to-square" />
    </div>

    {{ form_end(form) }}

</div>
