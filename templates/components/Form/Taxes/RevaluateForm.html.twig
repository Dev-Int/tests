<div>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.rate) }}
    {{  form_row(form.uuid) }}

    <div class="grid">
        <twig:Form:Button:Cancel href="{{ path('admin_taxes_index') }}" label="Annuler" />
        <twig:Form:Button:Submit label="Revaluate" />
    </div>

    {{ form_end(form) }}

</div>
