{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.familyLog.create.titlePage'|trans }} | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app','familyLog-select']) }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:FamilyLog /> {{ 'admin.familyLog.create.titlePage'|trans }}</h1>

    <turbo-frame id="familyLog_create">
        <twig:Form:FamilyLog:CreateForm form="{{ form }}" />
    </turbo-frame>

{% endblock %}
