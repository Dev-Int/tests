{% extends 'base.html.twig' %}

{% block title %}
    Update Tax | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Taxe /> Rename Tax "{{ tax.name }}"</h1>

    <turbo-frame id="tax_{{ tax.uuid }}">
        <twig:Form:Taxes:RenameForm form="{{ form }}" tax="{{ tax }}" />
    </turbo-frame>

{% endblock %}
