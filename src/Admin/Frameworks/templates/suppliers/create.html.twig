{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.supplier.create.titlePage'|trans }} | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app','familyLog-select']) }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Supplier /> {{ 'admin.supplier.create.titlePage'|trans }}</h1>

    <turbo-frame id="supplier_create">
        <twig:Form:Supplier:CreateForm form="{{ form }}" initialFormData="{{ form.vars.value }}" />
    </turbo-frame>

{% endblock %}
