{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.supplier.changeContact.titleShort'|trans }} | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Supplier /> {{ 'admin.supplier.changeContact.titlePage'|trans({ '%supplierName%': supplier.name }) }}</h1>

    <turbo-frame id="supplier_{{ supplier.uuid }}">
        <twig:Form:Supplier:ChangeContactForm form="{{ form }}" supplier="{{ supplier }}" />
    </turbo-frame>

{% endblock %}

