{% extends 'base.html.twig' %}

{% block title %}
    Update Unit | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Unit /> Change label "{{ unit.label }}"</h1>

    <turbo-frame id="unit_{{ unit.uuid }}">
        <twig:Form:Unit:ChangeLabelForm form="{{ form }}" unit="{{ unit }}" />
    </turbo-frame>

{% endblock %}

