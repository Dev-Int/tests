{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.article.create.titleShort'|trans }} | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app','familyLog-select']) }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Article /> {{ 'admin.article.create.titlePage'|trans }}</h1>

    <turbo-frame id="article_create">
        <twig:Form:Article:CreateForm form="{{ form }}" />
    </turbo-frame>

{% endblock %}
