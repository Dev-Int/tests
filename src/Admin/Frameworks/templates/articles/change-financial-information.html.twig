{% extends 'base.html.twig' %}

{% block title %}
    Change financial information Article | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Article /> Change financial information to "{{ article.name }}"</h1>

    <turbo-frame id="article_{{ article.uuid }}">
        <twig:Form:Article:ChangeFinancialInformationForm form="{{ form }}" article="{{ article }}" />
    </turbo-frame>

{% endblock %}

