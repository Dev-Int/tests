{% extends 'base.html.twig' %}

{% block title %}
    Update Company | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:Company /> CUpdate Company "{{ companyName }}"</h1>

    <turbo-frame id="company_update">
        <twig:Form:Company:UpdateForm form="{{ form }}" companyName="{{ companyName }}"/>
    </turbo-frame>

{% endblock %}
