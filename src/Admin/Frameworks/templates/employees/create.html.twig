{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.employee.create.titlePage'|trans }} | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app']) }}
{% endblock %}

{% block body %}

    <h1><i class="fa fa-users"></i> {{ 'admin.employee.create.titlePage'|trans }}</h1>

    <turbo-frame id="employee_create">
        <twig:Form:Employee:CreateForm form="{{ form }}" initialFormData="{{ form.vars.value }}" />
    </turbo-frame>

{% endblock %}
