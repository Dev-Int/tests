<div class="flex-1">

    <h3>{{ 'admin.familyLog.create.titlePage'|trans }}</h3>

    {{ form_start(form) }}
   {{ form_errors(form) }}

   {{ form_row(form.label) }}

   {{ include('partials/components/form/_familyLogs-select.html.twig', {familyLog: form.parent}) }}

   <div class="grid">
       <twig:Form:Button:Cancel href="{{ path('admin_family_logs_index') }}" label="{{ 'cancel'|trans }}" turboFrame="_top" />
       <twig:Form:Button:Submit label="{{ 'add'|trans }}" icon="fa-plus" />
   </div>

   {{ form_end(form) }}

</div>
