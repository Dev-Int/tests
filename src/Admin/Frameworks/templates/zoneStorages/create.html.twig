{% extends 'base.html.twig' %}

{% block title %}
    Create ZoneStorage | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app','familyLog-select']) }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:ZoneStorage /> Create Zone Storage</h1>

    <turbo-frame id="zoneStorage_create">
        <twig:Form:ZoneStorage:CreateForm form="{{ form }}" />
    </turbo-frame>

{% endblock %}
