{% extends 'base.html.twig' %}

{% block title %}
    {{ 'admin.zoneStorage.create.titlePage'|trans}} | {{ parent() }}
{% endblock %}

{% block importmap %}
    {{ importmap(['app','familyLog-select']) }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:ZoneStorage /> {{ 'admin.zoneStorage.create.titlePage'|trans }}</h1>

    <turbo-frame id="zoneStorage_create">
        <twig:Form:ZoneStorage:CreateForm form="{{ form }}" initialFormData="{{ form.vars.value }}" />
    </turbo-frame>

{% endblock %}
