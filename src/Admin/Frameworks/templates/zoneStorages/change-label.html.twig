{% extends 'base.html.twig' %}

{% block body %}

    <h1><i class="fa-solid fa-warehouse"></i> Change label "{{ zoneStorage.label }}"</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.label) }}

    <div class="grid">
        <a href="{{ path('admin_zone_storages_index') }}" class="secondary" role="button">Annuler</a>
        <button type="submit">Change label</button>
    </div>

    {{ form_end(form) }}

{% endblock %}

