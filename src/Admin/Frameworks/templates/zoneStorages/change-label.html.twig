{% extends 'base.html.twig' %}

{% block title %}
    Update ZoneStorage | {{ parent() }}
{% endblock %}

{% block body %}

    <h1><twig:Icon:Entities:ZoneStorage /> Change label "{{ zoneStorage.label }}"</h1>

    <turbo-frame id="zone_{{ zoneStorage.uuid }}">
        <h2>Change label "{{ zoneStorage.label }}"</h2>

        <twig:Form:ZoneStorage:ChangeLabelForm form="{{ form }}" />
    </turbo-frame>

{% endblock %}

