services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false
    
    Admin\:
        resource: '%kernel.project_dir%/src/Admin/*'
        exclude: 
            - '%kernel.project_dir%/src/Admin/{Frameworks,Tests,Entities}'
            - '%kernel.project_dir%/src/Admin/Adapters/Gateway/ORM/Entity'
